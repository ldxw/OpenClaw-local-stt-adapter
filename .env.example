# 本地 STT 服务端口
STT_PORT=8080

# Whisper 模型: base / small / medium
WHISPER_MODEL=small

# 设备: cpu / cuda
WHISPER_DEVICE=cpu

# CPU 推荐 int8，显卡常用 float16
WHISPER_COMPUTE_TYPE=int8

# 默认语言
DEFAULT_LANGUAGE=zh

# 鉴权 Token，务必改成自己的随机字符串
API_TOKEN=please_change_me_to_a_long_random_secret
